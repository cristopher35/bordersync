spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bordersync}
spring.datasource.username=${DB_USERNAME:bordersync}
spring.datasource.password=${DB_PASSWORD:bordersync}
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
